<script id="example-collection-template" type="text/x-handlebars-template">
  {{#each examples}}
  <a href="{{ url }}">
    <div class="uk-card uk-card-default uk-card-small uk-card-body uk-width-1-3@m">
      <h3 class="uk-card-title">{{ title }}</h3>
      <div class="uk-card-body">
        {{summary}}
      </div>
    </div>
  </a>
  {{/each}}
</script>